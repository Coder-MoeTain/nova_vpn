{
  "name": "novavpn-provisioning-server",
  "version": "1.0.0",
  "description": "Provisioning API backend for NovaVPN: registers devices and returns WireGuard config.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.0",
    "dotenv": "^16.4.5",
    "mysql2": "^3.11.0"
  }
}
